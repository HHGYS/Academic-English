# 学术英语回译 · 使用说明

一个纯网页的学习工具：**把读文献时遇到的好句子记下来（英文＋中文＋标签），练习时只看中文，凭记忆写出英文，系统逐词检查，每个单词都对了才过关。**

## 一、怎么用（三个页面）

1. **文章列表（首页）**
   - 每篇文章 = 一个「文件夹」，用论文题目命名
   - 「新建文章」→「新增句子」即可开始录入
   - 顶部显示：文章数、句子总数、已过关数

2. **文章详情**
   - 打开一篇文章，看到里面所有句子（默认只显示中文，点「显示英文」才展开，防止复习时瞟到答案）
   - 支持：新增/编辑/删除句子、加标签、搜索、按标签筛选、只看未过关
   - 句子可单独「练习此句」

3. **练习**
   - 只看中文 → 在输入框里凭记忆写出英文 → 提交
   - 输入框上方有**长度提示**：每个还没写的词用一排下划线显示大致长度（＿的数量＝该词长度），心里有数才写得出来
   - **边写边实时识别**：写对的词当场变绿、写错的当场变红，不用等提交就能看出来
   - 提交后系统逐词校对：**绿＝写对，红＝写错/漏掉/多写**，并给出原文对照
   - **每个单词都对 → 过关**，自动标记「已过关」
   - 支持：下一题、重练本题、跳过、直接看原文；顺序随机

## 二、判分规则（重点）

- 单词必须**完全一致**（拼写、顺序、数量）
- **大小写和句号逗号等标点不计较**（`Moreover,` 写成 `Moreover` 也算对）
- **撇号会计入**（`it's` 和 `its` 是不同词，别写混）

## 三、多设备使用（重要）

- 数据保存在**当前设备的浏览器**里，不同设备之间**不自动同步**
- **换设备：** 旧设备点「导出数据」→ 得到 `.json` 文件 → 把文件发到新设备 → 新设备打开网站点「导入数据」选择该文件
- 建议**定期导出备份**

## 四、部署到网上（免费，任意设备打开网址即用）

### 方式一：Netlify Drop（最简单，不用注册 GitHub）
1. 手机/电脑浏览器打开：<https://app.netlify.com/drop>
2. 把 `index.html` 这个文件直接拖进页面
3. 几秒后得到一个网址（形如 `https://xxxx.netlify.app`），之后任何设备打开这个网址就能用

### 方式二：GitHub Pages
1. GitHub 新建一个仓库（Public）
2. 上传 `index.html`
3. 仓库 **Settings → Pages → Source 选分支 → Save**
4. 得到网址 `https://你的用户名.github.io/仓库名/`，任何设备可访问

> 提示：部署后数据仍存在各设备的浏览器里（见第三节），用导出/导入同步即可。

### 不想部署？
把 `index.html` 直接存到手机/电脑上，双击用浏览器打开也一样能用（只是只在那台设备上有效）。
